package PkgConfig::LibPkgConf::Test;

use strict;
use warnings;
use base qw( Exporter );
use PkgConfig::LibPkgConf;

our $VERSION = '0.01';
$VERSION = eval $VERSION;
our @EXPORT_OK = qw( send_error );

1;
